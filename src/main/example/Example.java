package example;

import db.Connector;

public class Example {

    private static final Connector connector = new Connector("jdbc:hsqldb://localhost/test", "test", "test");

    /*
    Start with an example, with just a main method that creates a database and table, inserts some rows and queries or something.

     */
    public static void main(String[] args) {

    }
}
